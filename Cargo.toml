[package]
name = "ussisonad"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1.89"

[dev-dependencies]
tokio = { version = "1.50.0", features = ["macros", "rt"] }
tokio-test = "0.4.5"
